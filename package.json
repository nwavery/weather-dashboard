{
  "name": "weather-dashboard",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Time & Weather dashboard (React + Vite) served by a Cloud Run container that proxies pollen via a server-held key.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node server/index.js",
    "serve": "npm run build && node server/index.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "vite": "^5.4.11"
  },
  "engines": {
    "node": ">=20"
  }
}
